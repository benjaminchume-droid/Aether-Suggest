# Aether Suggest — optional provider keys
# None are required at build time. Configure at runtime or via localStorage.

VITE_OPENAI_API_KEY=
VITE_GEMINI_API_KEY=
# Groq / DeepSeek use the OpenAI-compatible path — set via UI or localStorage
